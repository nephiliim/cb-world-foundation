# V5 Homepage Fix

This update replaces only `app/page.tsx` with a clean homepage that uses existing CSS class names prefixed with `home-v5`.

After copying the file, append the CSS block from README to `app/globals.css`.
