# Install Command Centre V1

Copy the files into your project root.

Add this to `app/layout.tsx`:

```ts
import "./admin/command-centre.css";
```

Then run:

```cmd
npm run build
git add .
git commit -m "Add Command Centre V1 and project docs"
git push
```
